# This workflow builds an Angular project, runs tests and lint with a single version of Node.
name: Build Frontend

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Lint
        run: npm run lint
        working-directory: frontend

      - name: Test
        run: npm test -- --watch=false
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend

      - name: Prepare frontend artifact
        run: |
          mkdir -p out
          rm -f out/frontend.zip
          test -d frontend/dist/omdb-frontend
          zip -r out/frontend.zip frontend/dist/omdb-frontend

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-zip
          path: out/frontend.zip
          if-no-files-found: error
          retention-days: 3
